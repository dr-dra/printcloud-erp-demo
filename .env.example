# ============================================================================
# PRINTCLOUD ENVIRONMENT VARIABLES - EXAMPLE/TEMPLATE
# ============================================================================
# This is a template file. Copy it to create your actual environment files:
#
# FOR DEVELOPMENT:
#   1. Copy this file to backend/.env
#   2. Update the values below with your actual configuration
#   3. Make sure to change passwords and API keys!
#
# FOR PRODUCTION:
#   1. Copy this file to backend/.env
#   2. Set DEBUG=False
#   3. Use strong, unique passwords
#   4. Set proper ALLOWED_HOSTS with your domain
#   5. Consider using environment variable management tools
#
# SECURITY WARNING:
#   - Never commit .env files to git!
#   - Keep your API keys and passwords secret!
#   - Use different credentials for dev and production!
# ============================================================================

# ============================================================================
# DJANGO CORE SETTINGS
# ============================================================================

# Django secret key - MUST be unique and kept secret!
# Generate a new one: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-super-secret-key-change-this-in-production

# Debug mode - Set to False in production!
# True = Show detailed error pages (helpful for development)
# False = Hide error details (required for security in production)
DEBUG=True

# Allowed hosts - domains that can access your app
# Development: Use * for localhost
# Production: Set to your domain (e.g., yourdomain.com,www.yourdomain.com)
ALLOWED_HOSTS=*

# Frontend URL - Where your frontend is hosted
FRONTEND_URL=http://localhost:3000

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Primary database (PostgreSQL)
# When using Docker Compose, the values below are used:
# - postgres = service name (Docker resolves to container IP)
# - postgres123 = password (matches docker-compose.yml)
# - 5432 = PostgreSQL port
# - printcloud_db = database name
DATABASE_URL=postgres://postgres:postgres123@postgres:5432/printcloud_db

# PostgreSQL specific settings (for docker-compose.prod.yml)
POSTGRES_DB=printcloud_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123

# ============================================================================
# MYSQL LEGACY DATABASE (Optional)
# ============================================================================

# Enable/disable MySQL legacy database
# Set to True if you're using the old pressmanager_db
ENABLE_MYSQL_LEGACY=True

# MySQL connection details (for docker-compose)
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=pressmanager_db
MYSQL_USER=mysql
MYSQL_PASSWORD=mysql123

# MySQL root password (for docker-compose)
MYSQL_ROOT_PASSWORD=mysql123

# ============================================================================
# MAILSERVER DATABASE (External VPS)
# ============================================================================

# These settings connect to the external mailserver database
# Update with your actual mailserver database credentials
MAILSERVER_DB_NAME=mailserver
MAILSERVER_DB_USER=mailuser
MAILSERVER_DB_PASSWORD=your-mailserver-password
MAILSERVER_DB_HOST=mailserver.example.com
MAILSERVER_DB_PORT=5432

# ============================================================================
# REDIS CONFIGURATION (Celery Broker + Cache)
# ============================================================================

# Redis connection settings
# When using Docker Compose:
# - redis = service name
# - 6379 = default Redis port
# - 0 = database number (Redis supports 16 databases by default)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# ============================================================================
# EMAIL CONFIGURATION (AWS SES)
# ============================================================================

# Email backend - Django email system
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# AWS SES SMTP settings
# Get these from your AWS SES console
EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False

# AWS SES credentials
# SECURITY: Replace with your actual AWS SES credentials
EMAIL_HOST_USER=your-aws-ses-access-key
EMAIL_HOST_PASSWORD=your-aws-ses-secret-key

# Default "From" email address
DEFAULT_FROM_EMAIL=no-reply@yourdomain.com

# ============================================================================
# JWT AUTHENTICATION SETTINGS
# ============================================================================

# Access token lifetime (how long users stay logged in)
# Format: 5m = 5 minutes, 1h = 1 hour, 1d = 1 day
ACCESS_TOKEN_LIFETIME=5m

# Refresh token lifetime (how long before user must log in again)
REFRESH_TOKEN_LIFETIME=1d

# ============================================================================
# CORS SETTINGS (Cross-Origin Resource Sharing)
# ============================================================================

# Allow all origins? (useful for development, dangerous in production)
CORS_ALLOW_ALL_ORIGINS=False

# Specific allowed origins (comma-separated)
# Development: Include all ports you're using
# Production: Only your production frontend domain
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================================================
# AWS S3 STORAGE (File Uploads)
# ============================================================================

# AWS credentials
# Get these from your AWS IAM console
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# S3 bucket configuration
AWS_STORAGE_BUCKET_NAME=your-bucket-name
AWS_S3_REGION_NAME=ap-south-1
AWS_S3_FILE_OVERWRITE=False
AWS_DEFAULT_ACL=None
AWS_S3_ADDRESSING_STYLE=virtual

# ============================================================================
# DOCKER-SPECIFIC NOTES
# ============================================================================
#
# When running with Docker Compose, some variables are set differently:
#
# 1. DATABASE HOSTS:
#    - Use service names (postgres, mysql, redis) instead of localhost
#    - Docker's internal DNS resolves these to container IPs
#
# 2. PORTS:
#    - Containers can communicate on internal network without port mapping
#    - Example: backend can reach postgres at postgres:5432
#
# 3. ENVIRONMENT PRIORITY:
#    - docker-compose.yml can override values from .env
#    - Check docker-compose.yml for the final values used
#
# ============================================================================

# ============================================================================
# PRODUCTION-SPECIFIC SETTINGS
# ============================================================================
#
# When deploying to production, also set these in docker-compose.prod.yml:
#
# NEXT_PUBLIC_API_URL=https://yourdomain.com/api
#
# And make sure to:
# - Set DEBUG=False
# - Use strong, unique passwords
# - Set ALLOWED_HOSTS to your domain
# - Use HTTPS/SSL certificates
# - Keep this file secure (never commit to git!)
#
# ============================================================================
